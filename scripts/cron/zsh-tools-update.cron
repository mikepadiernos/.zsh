# Weekly policy-aware tooling update (Sunday 06:30)
# Install with: crontab -e
30 6 * * 0 cd "$HOME/.zsh" && ./scripts/update-tools.sh --scheduled --no-plasma --no-vscode >> "$HOME/.cache/zsh-tools-update.log" 2>&1
